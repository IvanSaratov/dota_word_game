# Dota Keyboard — быстрый запуск для Windows

`dota_keyboard.exe` распознаёт выбранную область мини-игры и, только при
включённом `live_input`, отправляет распознанный текст в окно игры. Сначала
обязательно проверьте сухой прогон: это защищает чат, HUD и другие элементы от
непреднамеренного ввода.

## Запуск и калибровка

1. Распакуйте `dota-keyboard-*-windows-x64.zip` в отдельную папку. Не
   перемещайте `dota_keyboard.exe`, `assets/models`, `config.json` и DLL из
   этой папки.
2. Переключите игру в безрамочный оконный режим (*borderless windowed*).
3. Запустите `dota_keyboard.exe` с тем же уровнем прав, что и игра: оба обычные
   либо оба запущены от администратора.
4. Переведите фокус в игру, нажмите **F7** и мышью обведите только область
   мини-игры. Не включайте в рамку HUD, чат, микро-подписи и соседний монитор.
   Калибровка сохраняется в `config.json`.

## Сухой прогон: F8 без ввода в игру

В поставляемом `config.json` указано `"live_input": false`.

1. Оставьте это значение `false`.
2. После калибровки нажмите **F8**. В консоли появится `RUNNING (DRY RUN)`;
   сверяйте распознанный нормализованный текст и значения confidence с целью
   на экране. В этом режиме программа не посылает клавиши в игру.
3. Нажмите **F8** ещё раз. Сообщение `STOPPED (dry run unchanged)` означает
   аварийную остановку до следующей отправки.

## Боевой режим: F8 с вводом

Переходите к нему только после успешного сухого прогона. В `config.json`
измените `"live_input": false` на `"live_input": true`, сохраните файл и
перезапустите утилиту. Снова сфокусируйте игру и нажмите **F8**. Консоль должна
написать `RUNNING (LIVE INPUT)`. **F8** в любой момент останавливает обработку;
сначала используйте его, если заметили неверное распознавание.

## Самостоятельная сборка на Windows 11 x64

Готовый ZIP удобнее скачивать из артефактов GitHub Actions. Для локальной
сборки установите:

- Visual Studio 2022 с workload **Desktop development with C++** и Windows SDK;
- Git;
- CMake 3.28 или новее;
- PowerShell 7 (`pwsh`);
- полную копию [vcpkg](https://github.com/microsoft/vcpkg), клонированную через
  Git, а не скачанную как ZIP.

Откройте PowerShell 7 в корне этого репозитория. В примере vcpkg находится в
`C:\src\vcpkg`; если у вас другой путь, измените только `$VcpkgRoot`.

```powershell
git clone https://github.com/microsoft/vcpkg.git C:\src\vcpkg
$VcpkgRoot = 'C:\src\vcpkg'
$Baseline = (Get-Content .\vcpkg.json -Raw | ConvertFrom-Json).'builtin-baseline'
git -C $VcpkgRoot fetch --no-tags origin $Baseline
git -C $VcpkgRoot checkout --detach $Baseline
& "$VcpkgRoot\bootstrap-vcpkg.bat" -disableMetrics
$env:VCPKG_ROOT = $VcpkgRoot
```

Если `C:\src\vcpkg` уже существует, первую команду `git clone` пропустите.
Затем загрузите закреплённые OCR-файлы, соберите Release, запустите тесты и
создайте пакет:

```powershell
pwsh -NoProfile -File .\scripts\fetch-models.ps1
cmake --preset windows-release
cmake --build --preset windows-release
ctest --preset windows-release --output-on-failure
cmake --install .\build\windows-release --config Release --prefix .\dist
cpack --preset windows-release
```

Первая сборка может занять много времени: vcpkg компилирует OpenCV и ONNX
Runtime. Не удаляйте `build\windows-release` и стандартный бинарный кэш vcpkg
`%LOCALAPPDATA%\vcpkg\archives` — повторные сборки с тем же `vcpkg.json` будут
быстрее. Готовый архив появится как
`build\windows-release\dota-keyboard-*-windows-x64.zip`.

## Неполадки

| Симптом | Что сделать |
| --- | --- |
| Ошибка модели или словаря | Убедитесь, что рядом с EXE есть `assets/models/en_PP-OCRv5_rec_mobile_infer.onnx` и `assets/models/ppocrv5_en_dict.txt`; распакуйте ZIP заново целиком. |
| Не найдена DLL | Не запускайте EXE из другой папки и не удаляйте DLL из ZIP. Распакуйте свежий архив и проверьте, что антивирус не поместил DLL в карантин. |
| F7/F8 ничего не делают или конфликтуют | Закройте программу, использующую эти глобальные горячие клавиши, либо задайте разные свободные виртуальные коды в `config.json` (`calibrate`/`toggle`) и перезапустите. |
| `DXGI_ERROR_ACCESS_LOST` | Остановите обработку F8, переключите игру обратно в borderless windowed после смены полноэкранного режима/драйвера и перезапустите утилиту. |
| Выбранная область пересекает мониторы | Нажмите F7 заново и выбирайте прямоугольник целиком на одном мониторе; захват не предназначен для пересечения границы дисплеев. |
| Игра запущена повышенно, а ввода нет | Это ограничение UIPI: перезапустите утилиту от администратора либо игру без повышения, чтобы уровни целостности совпали. |
| Низкая confidence или неверный текст | Повторите F7, исключите HUD/микро-текст, убедитесь в достаточном масштабе и контрасте; при необходимости не понижайте `min_ocr_confidence` до проверки сухого прогона. |
| Два кандидата или выбрана не та цель | Сузьте область F7 только до мини-игры. Для кандидатов на разных высотах применяется нижний; не включайте другие строки в ROI. |
| Пропущены или обрываются клавиши | Немедленно нажмите F8, проверьте фокус игры и отсутствие оверлеев. После сухого прогона можно осторожно увеличить `inter_key_delay_us` в `config.json`. |

## Приёмка в реальной игре

Ниже — чек-лист релиза. Он намеренно помечен как **не выполнен**: этот
репозиторий не подменяет проверку в реальной игре фиктивным результатом.
Тестируйте сначала с `live_input=false`, затем отдельно подтверждайте боевой
режим. В каждой строке ожидается: нет ввода HUD/микро-подписей, нормализованный
текст верен, при двух целях выигрывает нижняя, потеря фокуса блокирует ввод, а
F8 прекращает обработку до следующей отправки.

| Сценарий | Статус | Результат/заметка |
| --- | --- | --- |
| Короткое слово | Не выполнен | Заполнить после сухого прогона. |
| Длинное слово | Не выполнен | Заполнить после сухого прогона. |
| Фраза с пробелами | Не выполнен | Заполнить после сухого прогона. |
| Апостроф и дефис | Не выполнен | Заполнить после сухого прогона. |
| Цель у каждого края выбранной области | Не выполнен | Проверить все четыре края. |
| Две цели на разной высоте | Не выполнен | Подтвердить выбор нижней цели. |
| Частично жёлтая/белая цель | Не выполнен | Заполнить после сухого прогона. |
| Фокус переключён в другое окно | Не выполнен | Подтвердить отсутствие ввода. |
| Аварийная остановка F8 | Не выполнен | Подтвердить остановку до следующей отправки. |

## Бенчмарк перед выпуском

Для боевого профиля соберите не менее 300 подтверждённых целей и запишите mean
и p95 для `capture`, `detect`, `ocr` и `total` из консольной сводки. Цель
приёмки: p95 `total` не более 40 мс после доступности кадра. Результат пока не
собран; это не считается прохождением.

Если цель не достигнута, сначала проверьте Release-сборку, копирование только
выбранного ROI, повторное использование буферов и ONNX-сессии, снижение
конкуренции потоков ONNX и профилирование. Не ослабляйте подтверждение или
порог confidence ради скорости без отдельной проверки безопасности.
